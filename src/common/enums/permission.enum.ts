export enum PERMISSIONS_ENUM {
  CREATE = 'create',
  VIEW = 'view',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum TABLES_ENUM {
  DOCTOR = '_doctor',
  ROLE = '_role',
  SPECIALTY = '_specialty',
}
