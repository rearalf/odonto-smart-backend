export enum PERMISSIONS_ENUM {
  CREATE = 'create',
  VIEW = 'view',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum TABLES_ENUM {
  USER = '_user',
  ROLE = '_role',
  SPECIALTY = '_specialty',
}
